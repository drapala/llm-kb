# Chaves de API para o oracle externo (cross-model-challenge.py)
# Copie este arquivo para .env e preencha as chaves reais.
# .env está no .gitignore — nunca commitar.

OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AI...
